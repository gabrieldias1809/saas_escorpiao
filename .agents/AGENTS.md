# Project Rules

- Sempre que fizer alterações no código deste projeto, você deve executar `git add .`, `git commit` com uma mensagem descritiva e `git push` para subir as alterações para o GitHub. Não espere o usuário pedir.
